1) fix problem when deploy on ubuntu allowing users to create filesystem permission:
https://askubuntu.com/questions/703672/permission-denied-when-writing-a-file-to-new-filesystem
-> go to level same as uploads folder and input:
    sudo chown -R $USER:$USER ./uploads


2) allow npm to force install node-Sass:
https://github.com/sass/node-sass/issues/2178
bash: sudo npm install --unsafe-perm=true

3) create symbolic link from outside project folder uploads to inside project folder:
bash: sudo ln -s /var/www/uploads/ /var/www/sis_UiUx/

4) For deployment, delete /dist and /dist-server at Angular/.gitignore
    -> add back during development

5) Solve 413 Errors loading images more than default 1mb (aware when creating a new servers):
    -> Express Server-side: https://stackoverflow.com/questions/19917401/error-request-entity-too-large
    -> NginX Server: https://stackoverflow.com/questions/28476643/default-nginx-client-max-body-size